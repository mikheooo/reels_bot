[pytest]
markers =
    integration: marks tests as integration tests that require real DB/services (deselect with '-m "not integration"')
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
